server:
  port: 8081
spring:
  application:
    name: hmdp
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dianping?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8&useUnicode=true
    username: root
    password: wmz1005533
  data:
    redis:
      host: localhost
      port: 6379
      password:
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 1
        time-between-eviction-runs: 10s
  jackson:
    default-property-inclusion: non_null

  ai:
    openai:
      api-key: sk-cb8e34ac7699424c9bc044efedba3650
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          model: qwen-plus
      embedding:
        options:
          model: text-embedding-v3
    vectorstore:
      milvus:
        uri: http://localhost:19530
        collection-name: hmdp_knowledge
        index-type: HNSW
        metric-type: COSINE
        embedding-dimension: 1024
        initialize-schema: false

mybatis-plus:
  type-aliases-package: com.hmdp.entity
  mapper-locations: classpath*:/mapper/**/*.xml

logging:
  level:
    root: WARN
    com.hmdp: INFO
    org.springframework.ai: WARN
    org.springframework.web: INFO
    org.mybatis: WARN
    com.mysql: WARN
    org.redisson: WARN