server:
  port: 8081
spring:
  application:
    name: hmdp
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dianping?useSSL=false&serverTimezone=UTC
    username: root
    password: wmz1005533
  data:
    redis:
      host: localhost
      port: 6379
      password:
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 1
        time-between-eviction-runs: 10s
  jackson:
    default-property-inclusion: non_null

langchain4j:
  open-ai:
    chat-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: sk-cb8e34ac7699424c9bc044efedba3650
      model-name: qwen-plus
      log-requests: true
      log-responses: true
    streaming-chat-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: sk-cb8e34ac7699424c9bc044efedba3650
      model-name: qwen-plus
      log-requests: true
      log-responses: true
    embedding-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: sk-cb8e34ac7699424c9bc044efedba3650
      model-name: text-embedding-v3
      log-requests: true
      log-responses: true
      max-segments-per-batch: 10
  
  # Milvus 向量数据库配置
  milvus:
    host: localhost
    port: 19530
    username: 
    password: 
    database: default
    collection-name: hmdp_knowledge
    index-type: HNSW
    metric-type: COSINE

mybatis-plus:
  type-aliases-package: com.hmdp.entity
  mapper-locations: classpath*:/mapper/**/*.xml

logging:
  level:
    root: WARN  # 根日志级别为 WARN，只输出警告和错误
    com.hmdp: INFO  # 应用程序包输出 INFO 级别
    dev.langchain4j: WARN  # LangChain4j 只输出警告
    org.springframework.web: INFO  # Spring Web 输出 INFO
    org.mybatis: WARN  # MyBatis 只输出警告
    com.mysql: WARN  # MySQL 驱动只输出警告
    org.redisson: WARN  # Redisson 只输出警告